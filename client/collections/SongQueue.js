// SongQueue.js - Defines a backbone model class for the song queue.
var SongQueue = Songs.extend({


  initialize: function(songs){

  },

  // add: function(song){
  // 	// this.model.get('songQueue').add(song);
  //   console.log(this.get('songlist'));
  //   this.set({'songlist': this.get('songlist').add(song)});
  // },

  // remove: function(song){
  // 	// this.remove(song);
  //   var songCollection = this.get('songlist');
  //   var songIndex = songCollection.indexOf(song);
  //   this.set({'songlist': songCollection.splice(songIndex, 1)});
  // },

  // playFirst: function(){
  //   // console.log("Hi", this.get(0));
  //   // this.at(0).play();
  //   // this.set('currentSong', song);

  //   // this.set('currentSong', this.get('songQueue').at(0));
  // }


});